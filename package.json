{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "npm run generate && git checkout build && git rm -rf . && cp -r dist/* . && git add . && git commit -m 'Deploy build' && git push origin build && git checkout main"

  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.3",
    "lightgallery": "^2.8.2",
    "nuxt": "^3.15.2",
    "nuxt-swiper": "^2.0.0",
    "sass-loader": "^16.0.4",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.56.0"
  }
}
